import React from "react";
import Paper from "../../Surfaces/Paper";

const DashedPaper = ({ children }) => {
  return (
    <Paper
      sx={{
        display: "flex",
        justifyContent: "center",
        borderRadius: "20px",
        border: "2px dashed #919EAB40",
        padding: "24px",
        boxShadow: "none",
        backgroundColor: (theme) =>
        theme.palette.mode === "dark" ? "#1A2027" : "#FBFBFC",
      }}
    >
      {children}
    </Paper>
  );
};

export default DashedPaper;
