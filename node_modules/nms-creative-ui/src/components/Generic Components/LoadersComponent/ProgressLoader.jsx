import { Backdrop } from "@mui/material";
import React from "react";
import Box from "../../Layout/Box";
// import LinearProgress from "../../Feedback/Progress/LinearProgress";

const ProgressLoader = ({ open }) => {
  return (
    <Backdrop open={open} style={{ zIndex: 9999 }}>
      <Box
        sx={{ width: "150px", mr: 1, minWidth: "120px", textAlign: "center" }}
      >
        <img
          src="/public/nmsLogo.svg"
          alt="nms logo"
          style={{ paddingBottom: 10 }}
          className="logo-animation"
        />
        {/* <LinearProgress
          variant="indeterminate"
          sx={{
            borderRadius: "10px",
          }}
        /> */}
      </Box>
    </Backdrop>
  );
};

export default ProgressLoader;
