import React from "react";
import { FormGroup as NMSFormGroup } from "@mui/material";

const FormGroup = ({ children, sx, ...props }) => {
  return (
    <NMSFormGroup sx={sx} {...props}>
      {children}
    </NMSFormGroup>
  );
};

export default FormGroup;
