import React from "react";
import { FormLabel as NMSFormLabel } from "@mui/material";

const FormLabel = ({ children, color, component, sx, ...props }) => {
  return (
    <NMSFormLabel color={color} component={component} sx={sx} {...props}>
      {children}
    </NMSFormLabel>
  );
};

export default FormLabel;
