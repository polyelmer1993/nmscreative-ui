// ParentComponent.js
import React, { useState } from "react";

import { Outlet } from "react-router-dom";
import AppBar from "../components/Generic Components/AppBar Component/AppBar";
import { Container, CssBaseline, Box, Toolbar } from "@mui/material";
import SideBar from "../components/Generic Components/Sidebar Components/SideBar";


const Base = ({ darkMode, toggleDarkMode }) => {
  const [mobileOpen, setMobileOpen] = useState(false);
  const [isClosing, setIsClosing] = useState(false);

  const handleDrawerSideClose = () => {
    setOpen(false);
  };
  const handleDrawerClose = () => {
    setIsClosing(true);
    setMobileOpen(false);
  };

  const handleDrawerTransitionEnd = () => {
    setIsClosing(false);
  };

  const handleDrawerToggle = () => {
    setMobileOpen(prevMobileOpen => !prevMobileOpen);
  };

  return (
    <Box sx={{ display: "flex" }}>
      <CssBaseline />
      <AppBar
        handleDrawerToggle={handleDrawerToggle}
        darkMode={darkMode}
        toggleDarkMode={toggleDarkMode}
      />
      {/* Updated here */}
      <SideBar
        mobileOpen={mobileOpen}
        handleDrawerSideClose={handleDrawerSideClose}
        handleDrawerClose={handleDrawerClose}
        handleDrawerTransitionEnd={handleDrawerTransitionEnd}
      />
      {/* Updated here */}
      <Box
        component="main"
        sx={{
          width: "100%",
          // flexGrow: 1,
          padding: { mobile: "24px",tablet: "48px", desktop: "48px" },
          paddingTop: { mobile: "48px",tablet: "48px", desktop: "48px" },
        }}
      >
        <Toolbar />
        <Container maxWidth="desktop" disableGutters>
          <Outlet />
        </Container>
      </Box>
    </Box>
  );
};


export default Base;

