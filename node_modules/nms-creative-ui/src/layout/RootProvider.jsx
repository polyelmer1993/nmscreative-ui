import React from 'react'
import { ThemeProvider } from '../components'
import SnackbarProvider from '../components/Feedback/Snackbar/SnackbarProvider/SnackbarProvider'


const RootProvider = ({children}) => {
  return (
   <ThemeProvider>
    <SnackbarProvider
    anchorOrigin={{
        horizontal: 'right',
        vertical: 'top'
    }}>

    </SnackbarProvider>
   </ThemeProvider>
  )
}

export default RootProvider