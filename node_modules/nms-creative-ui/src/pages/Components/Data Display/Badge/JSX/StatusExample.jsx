export const StatusExample = `import React from "react";
import { Avatar, Badge, Icon, Stack } from "nms-creative-ui";
import * as SolarIconSet from "solar-icon-set";
import { styled } from "@mui/material";

const Status = () => {
  const StyledBadge = styled(Badge)(({ theme }) => ({
    "& .MuiBadge-badge": {
      backgroundColor: "#FF4545",
      color: "#FF4545",
      boxShadow: \`0 0 0 2px \${theme.palette.background.paper}, 0 0 0 4px #FF4545\`,
      "&::after": {
        position: "absolute",
        top: 0,
        left: 0,
        width: "100%",
        height: "100%",
        borderRadius: "50%",
        animation: "ripple 1.2s infinite ease-in-out",
        border: "1px solid currentColor",
        content: '""',
      },
    },
    "@keyframes ripple": {
      "0%": {
        transform: "scale(.8)",
        opacity: 1,
      },
      "100%": {
        transform: "scale(2.4)",
        opacity: 0,
      },
    },
  }));

  return (
    <Stack direction="row" spacing={4}>
      <StyledBadge
        anchorOrigin={{
          vertical: "bottom",
          horizontal: "right",
        }}
        variant="dot"
        color={"primary"}
        overlap="circular"
      >
        <Avatar />
      </StyledBadge>

      <Badge
        anchorOrigin={{
          vertical: "bottom",
          horizontal: "right",
        }}
        variant="dot"
        color={"primary"}
        overlap="circular"
        badgeContent={
          <Icon size={24} solariconset={SolarIconSet.ClockCircle} />
        }
      >
        <Avatar />
      </Badge>

      <Badge
        anchorOrigin={{
          vertical: "bottom",
          horizontal: "right",
        }}
        variant="dot"
        color={"primary"}
        overlap="circular"
      >
        <Avatar />
      </Badge>
    </Stack>
  );
};

export default Status;`