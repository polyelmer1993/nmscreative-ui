export const DeletableExample = `import React, { Fragment } from "react";
import { Chip, Stack } from "nms-creative-ui";

const Deletable = () => {
  const handleClick = () => {
    console.info("You clicked the Chip.");
  };

  const handleDelete = () => {
    console.info("You clicked the delete icon.");
  };

  return (
    <Fragment>
      <Stack direction="row" spacing={1}>
        <Chip
          label="Clickable Deletable"
          onClick={handleClick}
          onDelete={handleDelete}
          clickable
        />
        <Chip
          label="Clickable Deletable"
          variant="outlined"
          onClick={handleClick}
          onDelete={handleDelete}
          clickable
        />
      </Stack>
    </Fragment>
  );
};

export default Deletable;`
