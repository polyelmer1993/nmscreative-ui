import React from "react";
import * as SolarIconSet from "solar-icon-set";
import Icon from "../../../../../components/Foundation/Icon/Icon";
import {
  Button,
  SnackbarContent,
  Stack,
} from "../../../../../components";

const Customization = () => {
  // info action
  const info = (
    <Stack direction="row" spacing={0}>
      <Button variant="text" color="info">
        alert
      </Button>
      <Button variant="text" color="info">
        dismiss
      </Button>
    </Stack>
  );

  // warning action
  const warning = (
    <Stack direction="row" spacing={0}>
      <Button variant="text" color="warning">
        alert
      </Button>
      <Button variant="text" color="warning">
        dismiss
      </Button>
    </Stack>
  );

  // danger action
  const error = (
    <Stack direction="row" spacing={0}>
      <Button variant="text" color="error">
        alert
      </Button>
      <Button variant="text" color="error">
        dismiss
      </Button>
    </Stack>
  );

  // success action
  const success = (
    <Stack direction="row" spacing={0}>
      <Button variant="text" color="success">
        alert
      </Button>
      <Button variant="text" color="success">
        dismiss
      </Button>
    </Stack>
  );

  return (
    <Stack spacing={2}>
      <SnackbarContent
        message="Info"
        severity="info"
        color="info"
        icon={
          <Icon
            color={"info"}
            size={24}
            iconStyle={"BoldDuotone"}
            solariconset={SolarIconSet.InfoSquare}
          />
        }
        action={info}
      />
      <SnackbarContent
        message="Warning"
        severity="warning"
        color="warning"
        icon={
          <Icon
            color={"warning"}
            size={24}
            iconStyle={"BoldDuotone"}
            solariconset={SolarIconSet.InfoSquare}
          />
        }
        action={warning}
      />
      <SnackbarContent
        message="Error"
        severity="error"
        color="error"
        icon={
          <Icon
            color={"error"}
            size={24}
            iconStyle={"BoldDuotone"}
            solariconset={SolarIconSet.InfoSquare}
          />
        }
        action={error}
      />
      <SnackbarContent
        message="Success"
        severity="success"
        color="success"
        icon={
          <Icon
            color={"success"}
            size={24}
            iconStyle={"BoldDuotone"}
            solariconset={SolarIconSet.InfoSquare}
          />
        }
        action={success}
      />
    </Stack>
  );
};

export default Customization;
