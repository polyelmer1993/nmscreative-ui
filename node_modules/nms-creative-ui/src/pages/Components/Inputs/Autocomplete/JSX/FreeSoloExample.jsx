export const FreeSoloExample = `import React, { Fragment } from "react";
import { Autocomplete, Grid } from "nms-creative-ui";

const FreeSolo = () => {
  const options = [
    {
      icon: "",
      label: "Option 1",
      value: 1,
    },
    {
      icon: "",
      label: "Option 2",
      value: 2,
    },
    {
      icon: "",
      label: "Option 3",
      value: 3,
    },
  ];
  return (
    <Fragment>
      <Grid container spacing={2}>
        <Grid item desktop={6}>
          <Autocomplete
            id="free-solo"
            freeSolo
            options={options}
            label="Free Solo"
            renderInput={(params) => <TextField {...params} />}
          />
        </Grid>
        <Grid item desktop={6}>
          <Autocomplete
            id="free-solo"
            freeSolo
            disableClearable
            label="Search Input"
            options={options}
            renderInput={(params) => <TextField {...params} />}
          />
        </Grid>
      </Grid>
    </Fragment>
  );
};

export default FreeSolo;`;
