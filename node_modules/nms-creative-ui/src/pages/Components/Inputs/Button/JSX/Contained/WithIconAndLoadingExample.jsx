export const WithIconAndLoadingExample = `import React, { Fragment } from "react";
import { Button } from "nms-creative-ui";
import * as SolarIconSet from "solar-icon-set";

const WithIconAndLoading = () => {
  return (
    <Fragment>
      <Button
        color="secondary"
        startIcon={<SolarIconSet.CloudUpload size={24} />}
      >
        Upload file
      </Button>
      <Button
        startIcon={<SolarIconSet.TrashBinTrash size={24} />}
        color="error"
      >
        Medium
      </Button>
      <Button endIcon={<SolarIconSet.AltArrowDown size={24} />}>
        Icon Right
      </Button>
    </Fragment>
  );
};

export default WithIconAndLoading;`;
