export const BasicIconButtonExample = `import React, { Fragment } from "react";
import {
  IconButton
} from "nms-creative-ui";
import * as SolarIconSet from "solar-icon-set";

const Basic = () => {
  return (
    <Fragment>
      <IconButton variant="text">
        <SolarIconSet.Alarm size={24} iconStyle="BoldDuotone" />
      </IconButton>
      <IconButton variant="text" color={"primary"}>
        <SolarIconSet.Alarm size={24} iconStyle="BoldDuotone" />
      </IconButton>
      <IconButton variant="text" color="secondary">
        <SolarIconSet.Alarm size={24} iconStyle="BoldDuotone" />
      </IconButton>
      <IconButton variant="text" disabled>
        <SolarIconSet.Alarm size={24} iconStyle="BoldDuotone" />
      </IconButton>
    </Fragment>
  );
};

export default Basic;`
