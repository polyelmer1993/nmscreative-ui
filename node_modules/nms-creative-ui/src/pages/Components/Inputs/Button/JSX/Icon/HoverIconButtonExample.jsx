export const HoverIconButtonExample = `import React, { Fragment } from "react";
import { IconButton } from "nms-creative-ui";
import * as SolarIconSet from "solar-icon-set";

const Hover = () => {
  return (
    <Fragment>
      <IconButton variant="text" color="info" size="small">
        <SolarIconSet.Alarm size={18} iconStyle="BoldDuotone" />
      </IconButton>
      <IconButton variant="text" color="info" size="medium">
        <SolarIconSet.Alarm size={24} iconStyle="BoldDuotone" />
      </IconButton>
      <IconButton variant="text" color="info" size="large">
        <SolarIconSet.Alarm size={28} iconStyle="BoldDuotone" />
      </IconButton>
    </Fragment>
  );
};

export default Hover;`
