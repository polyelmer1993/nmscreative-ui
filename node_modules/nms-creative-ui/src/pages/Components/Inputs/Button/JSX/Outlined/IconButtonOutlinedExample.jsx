export const IconButtonOutlinedExample = `import React from "react";
import { Button } from "nms-creative-ui";
import * as SolarIconSet from "solar-icon-set";
import { Fragment } from "react";

const WithIconAndLoading = () => {
  return (
    <Fragment>
      <Button
        color="secondary"
        variant="outlined"
        startIcon={<SolarIconSet.CloudUpload size={24} />}
      >
        Upload file
      </Button>
      <Button
        variant="outlined"
        startIcon={<SolarIconSet.TrashBinTrash size={24} />}
        color="error"
      >
        Medium
      </Button>
      <Button
        variant="outlined"
        endIcon={<SolarIconSet.AltArrowDown size={24} />}
      >
        Icon Right
      </Button>
    </Fragment>
  );
};

export default WithIconAndLoading;`
