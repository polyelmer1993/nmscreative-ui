export const BasicTextButtonExample = `import React, { Fragment } from "react";
import { Button } from "nms-creative-ui";

const Basic = () => {
  return (
    <Fragment>
      <Button variant="text">Default</Button>
      <Button variant="text" color="secondary">
        Secondary
      </Button>
      <Button variant="text" disabled>
        Disabled
      </Button>
      <Button variant="text">Link</Button>
    </Fragment>
  );
};

export default Basic;`
