export const IconTextButtonExample = `import React from "react";
import { Button, Stack } from "nms-creative-ui";
import * as SolarIconSet from "solar-icon-set";

const WithIconAndLoading = () => {
  return (
    <Stack direction="row" spacing={2}>
      <Button
        color="secondary"
        variant="outlined"
        startIcon={<SolarIconSet.CloudUpload size={24} />}
      >
        upload file
      </Button>
      <Button
        variant="outlined"
        startIcon={<SolarIconSet.TrashBinTrash size={24} />}
        color="error"
      >
        medium
      </Button>
      <Button
        variant="outlined"
        endIcon={<SolarIconSet.AltArrowDown size={24} />}
      >
        Icon Right
      </Button>
    </Stack>
  );
};

export default WithIconAndLoading;`
