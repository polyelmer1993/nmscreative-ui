export const BasicToggleButtonExample = `import React, { Fragment, useState  } from "react";
import {
  Icon,
  ToggleButton,
  ToggleButtonGroup,
} from "nms-creative-ui";
import * as SolarIconSet from "solar-icon-set";

const Basic = () => {
  const [formats, setFormats] = useState(() => ['bold']);

  const handleFormat = (event, newFormats) => {
    setFormats(newFormats);
  };

  return (
    <Fragment>
      <ToggleButtonGroup
        value={formats}
        color="primary"
        exclusive
        onChange={handleFormat}
        aria-label="text formatting"
      >
        <ToggleButton value="bold" aria-label="bold">
          <Icon
            solariconset={SolarIconSet.TextBold}
            size={24}
            iconStyle={"Outline"}
          />
        </ToggleButton>

        <ToggleButton value="italic" aria-label="italic">
          <Icon
            solariconset={SolarIconSet.TextItalic}
            size={24}
            iconStyle={"Outline"}
          />
        </ToggleButton>

        <ToggleButton value="underlined" aria-label="underlined">
          <Icon
            solariconset={SolarIconSet.TextUnderline}
            size={24}
            iconStyle={"Outline"}
          />
        </ToggleButton>

        <ToggleButton value="eraser" disabled aria-label="eraser">
          <Icon
            solariconset={SolarIconSet.Eraser}
            size={24}
            color={"disabled"}
            iconStyle={"Outline"}
          />
        </ToggleButton>
      </ToggleButtonGroup>
    </Fragment>
  );
};

export default Basic;`
