import React, { Fragment } from "react";
import { IconButton } from "../../../../../../components";
import * as SolarIconSet from "solar-icon-set";

const Sizes = () => {
  return (
    <Fragment>
      <IconButton variant="text" color="warning" size="small">
        <SolarIconSet.Alarm size={18} iconStyle="BoldDuotone" />
      </IconButton>
      <IconButton variant="text" color="warning" size="medium">
        <SolarIconSet.Alarm size={24} iconStyle="BoldDuotone" />
      </IconButton>
      <IconButton variant="text" color="warning" size="large">
        <SolarIconSet.Alarm size={28} iconStyle="BoldDuotone" />
      </IconButton>
    </Fragment>
  );
};

export default Sizes;
