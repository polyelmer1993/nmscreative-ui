export const LabelPlacementExample = `import React from "react";
import { FormControlLabel, Radio, Stack } from "nms-creative-ui";

const LabelPlacement = () => {
  return (
    <Stack direction={"row"} spacing={4}>
      <FormControlLabel control={<Radio />} label="End" labelPlacement="end" />
      <FormControlLabel
        control={<Radio />}
        label="Start"
        labelPlacement="start"
      />
      <FormControlLabel control={<Radio />} label="Top" labelPlacement="top" />
      <FormControlLabel
        control={<Radio />}
        label="Bottom"
        labelPlacement="bottom"
      />
    </Stack>
  );
};

export default LabelPlacement;`;
