export const FullwidthExample = `import React, { Fragment } from "react";
import { Grid, TextField } from "nms-creative-ui";

const Fullwidth = () => {
  return (
    <Fragment>
      <Grid
        container
        spacing={2}
        sx={{ display: "flex", justifyContent: "center" }}
      >
        <Grid item desktop={12}>
          <TextField fullWidth label="outlined" variant="outlined" />
        </Grid>

        <Grid item desktop={12}>
          <TextField fullWidth label="filled" variant="filled" />
        </Grid>
        <Grid item desktop={12}>
          <TextField fullWidth label="Standard" variant="standard" />
        </Grid>
      </Grid>
    </Fragment>
  );
};

export default Fullwidth;`;
