export const MultilineExample = `import React, { Fragment } from "react";
import { TextField } from "nms-creative-ui";

const Multiline = () => {
  return (
    <Fragment>
      <TextField
        fullWidth
        label="Multiline"
        variant="outlined"
        placeholder="Multiline"
      />
      <TextField
        fullWidth
        label="Multiline"
        variant="outlined"
        placeholder="Multiline"
        multiline
      />
      <TextField
        fullWidth
        label="Multiline"
        variant="outlined"
        value="hehe"
        multiline
      />
    </Fragment>
  );
};

export default Multiline;`;
