import React, { Fragment } from "react";
import {TextField } from "../../../../../components";

const Form = () => {
  return (
    <Fragment>
      <TextField fullWidth label="Required" variant="outlined" required />

      <TextField
        fullWidth
        label="Disabled"
        variant="outlined"
        placeholder="disable"
        disabled
      />

      <TextField
        fullWidth
        label="Password"
        variant="outlined"
        type="password"
      />

      <TextField
        fullWidth
        label="Read Only"
        variant="outlined"
        readOnly
        placeholder="Read Only"
      />

      <TextField fullWidth label="Number" variant="outlined" type="number" />

      <TextField fullWidth label="Search" variant="outlined" type="search" />

      <TextField
        fullWidth
        variant="outlined"
        label="Helper Text"
        helperText="Helper Text"
      />
    </Fragment>
  );
};

export default Form;
