import React, { Fragment } from "react";
import { Box, Grid, Icon, TextField } from "../../../../../components";
import * as SolarIconSet from "solar-icon-set";

const Icons = () => {
  return (
    <Fragment>
      <TextField
        fullWidth
        label="With a start adornment"
        variant="standard"
        placeholder=""
        startAdornment={
          <Icon color="secondary">
            <SolarIconSet.UserRounded size={24} iconStyle="BoldDuotone" />
          </Icon>
        }
      />
      <TextField
        fullWidth
        label="TextField"
        variant="standard"
        placeholder=""
        startAdornment={
          <Icon color="secondary">
            <SolarIconSet.UserRounded size={24} iconStyle="BoldDuotone" />
          </Icon>
        }
      />
      <Box
        sx={{
          display: "flex",
          alignItems: "flex-end",
          gap: "4px",
        }}
      >
        <Icon color="secondary">
          <SolarIconSet.UserRounded size={24} iconStyle="BoldDuotone" />
        </Icon>

        <TextField fullWidth label="with sx" variant="standard" />
      </Box>
    </Fragment>
  );
};

export default Icons;
