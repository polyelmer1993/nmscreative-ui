export const BasicsExample = `import React from "react";
import { Box, ListItem, Paper, Stack } from "nms-creative-ui";
import { styled } from '@mui/material/styles';

const Item = styled(Paper)(({ theme }) => ({
  backgroundColor: theme.palette.mode === 'dark' ? '#638BCC' : '#FF4545',
  ...theme.typography.body2,
  padding: theme.spacing(1),
  textAlign: 'center',
  color: theme.palette.white.main,
}));
const Basics = () => {
  return (
    <Box sx={{ width: "100%" }}>
      <Stack spacing={2}>
        <Item>Item 1</Item>
        <Item>Item 2</Item>
        <Item>Item 3</Item>
      </Stack>
    </Box>
  );
};

export default Basics;`