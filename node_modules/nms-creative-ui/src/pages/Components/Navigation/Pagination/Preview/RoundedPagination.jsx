import React from "react";
import { Pagination, Stack } from "../../../../../components";

const RoundedPagination = () => {
  return (
    <Stack spacing={2}>
      <Pagination count={10} shape="rounded" siblingCount={0} />
      <Pagination
        count={10}
        variant="outlined"
        shape="rounded"
        color="primary"
        siblingCount={0}
      />
    </Stack>
  );
};

export default RoundedPagination;
