import React from "react";
import {
  Box,
  Card,
  CardContent,
  CardMedia,
  IconButton,
  Typography,
} from "../../../../../components";
import * as SolarIconSet from "solar-icon-set";
import { useTheme } from "@mui/material";

const UIControls = () => {
  const theme = useTheme();

  return (
    <Card sx={{ display: "flex" }}>
      <Box sx={{ display: "flex", flexDirection: "column" }}>
        <CardContent sx={{ flex: "1 0 auto" }}>
          <Typography component="div" variant="h5">
            Live From Space
          </Typography>
          <Typography
            variant="subtitle1"
            color="text.secondary"
            component="div"
          >
            Mac Miller
          </Typography>
        </CardContent>
        <Box sx={{ display: "flex", alignItems: "center", pl: 1, pb: 1, gap: "8px" }}>
          <IconButton aria-label="previous">
            {theme.direction === "rtl" ? (
              <SolarIconSet.SkipNext size={16} iconStyle="BoldDuotone" />
            ) : (
              <SolarIconSet.SkipPrevious size={16} iconStyle="BoldDuotone" />
            )}
          </IconButton>
          <IconButton aria-label="play/pause">
            <SolarIconSet.Play size={24} iconStyle="BoldDuotone" />
          </IconButton>
          <IconButton aria-label="next">
            {theme.direction === "rtl" ? (
              <SolarIconSet.SkipPrevious size={16} iconStyle="BoldDuotone" />
            ) : (
              <SolarIconSet.SkipNext size={16} iconStyle="BoldDuotone" />
            )}
          </IconButton>
        </Box>
      </Box>
      <CardMedia
        component="img"
        sx={{ width: 151 }}
        image="/images/cover-cardsample.jpg"
        alt="Live from space album cover"
      />
    </Card>
  );
};

export default UIControls;
