export const BoldSample = `import React from "react";
import * as SolarIconSet from "solar-icon-set";
import { Icon, Stack } from "../../../../../components";

const Bold = () => {
  return (
    <Stack direction={"row"} spacing={2}>
    <Icon>
      <SolarIconSet.Calendar size={24} iconStyle="Bold" />
    </Icon>

    <Icon color={"disabled"}>
      <SolarIconSet.Calendar size={24} iconStyle="Bold" />
    </Icon>

    <Icon color={"primary"}>
      <SolarIconSet.Calendar size={24} iconStyle="Bold" />
    </Icon>

    <Icon color={"secondary"}>
      <SolarIconSet.Calendar size={24} iconStyle="Bold" />
    </Icon>

    <Icon color={"info"}>
      <SolarIconSet.Calendar size={24} iconStyle="Bold" />
    </Icon>

    <Icon color={"success"}>
      <SolarIconSet.Calendar size={24} iconStyle="Bold" />
    </Icon>

    <Icon color={"warning"}>
      <SolarIconSet.Calendar size={24} iconStyle="Bold" />
    </Icon>

    <Icon color={"error"}>
      <SolarIconSet.Calendar size={24} iconStyle="Bold" />
    </Icon>
  </Stack>
  )
}

export default Bold`;