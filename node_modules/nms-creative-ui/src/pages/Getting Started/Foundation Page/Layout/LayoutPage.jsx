import React from "react";
import { Box, Icon, Stack, Typography } from "../../../../components";
import * as SolarIconSet from "solar-icon-set";
import Main from "./Preview/Main";
import { useMediaQuery } from "@mui/material";

const LayoutPage = () => {
  const isMobile = useMediaQuery("(max-width:600px)");
  return (
    <Stack spacing={6}>
      <Box>
        <Stack direction={"column"} spacing={3}>
          <Stack direction={"row"} spacing={1}>
            <Icon color="secondary">
              <SolarIconSet.BoxMinimalistic size={24} iconStyle="BoldDuotone" />
            </Icon>

            <Typography variant="body1">Foundation</Typography>
          </Stack>

          <Stack direction={"column"} spacing={1}>
            <Typography variant="h2">Layout</Typography>
            <Typography variant="body1">
              All the available layout in NMS Creative UI
            </Typography>
          </Stack>
        </Stack>
      </Box>
     
        <Main />
     
    </Stack>
  );
};

export default LayoutPage;
