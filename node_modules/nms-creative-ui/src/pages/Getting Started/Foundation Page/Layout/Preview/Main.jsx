import { Typography } from "@mui/material";
import React, { Fragment } from "react";
import {
  Box,
  Button,
  Card,
  CardActions,
  CardContent,
  Grid,
  Stack,
} from "../../../../../components";
import { Link } from "react-router-dom";
import { CARD_CONTENT } from "../data/cardContent";

const Main = () => {
  return (
    <Grid container>
      {CARD_CONTENT.map((cardContent) => (
        <Grid item mobile={12} tablet={4} desktop={4} padding= "24px">
          <Card sx={{  padding: "24px" }}>
            <Box sx={{ display: "flex", justifyContent: "center" }}>
              <img height={"100%"} width={"100%"} src={cardContent.cardMedia} />
            </Box>
            <CardContent>
              <Typography variant="h5">{cardContent.cardTitle}</Typography>
              <Typography variant="body1">{cardContent.cardContent}</Typography>
            </CardContent>
            <CardActions sx={{ display: "flex", justifyContent: "end" }}>
              <Link to={cardContent.path}>
                <Button size="medium" variant="text">
                  Learn More
                </Button>
              </Link>
            </CardActions>
          </Card>
        </Grid>
      ))}
    </Grid>
  );
};

export default Main;
