import { SvgIcon } from "@mui/material";
import React from "react";
import { BROWSERS } from "../../config/browserList";
import {
  Box,
  Card,
  CardContent,
  Grid,
  Stack,
  Typography,
} from "../../components";

const SupportedPlatformsPage = () => {
  return (
    <Box>
      <Stack direction={"column"} spacing={6}>
        <Box>
          <Stack direction={"column"} spacing={1}>
            <Typography variant="h2">Supported Platforms</Typography>
            <Typography variant="body1">
              Familiarize yourself with the range of platforms, spanning from
              contemporary to legacy, that are compatible with NMS Creative UI.
            </Typography>
          </Stack>
        </Box>

        <Box>
          <Stack direction={"column"} spacing={1}>
            <Typography variant="h3">Browser</Typography>
            <Box sx={{ flexGrow: 1 }}>
              <Grid
                container
                spacing={{ mobile: 2, laptop: 6 }}
                columns={{ mobile: 2, tablet: 3, laptop: 4, desktop: 5 }}
              >
                {BROWSERS.map((item) => (
                  <Grid item mobile={12} tablet={1}>
                    <Card
                      sx={{
                        "&.MuiPaper-elevation1": {
                          padding: "24px",
                        },
                      }}
                    >
                      <CardContent
                        sx={{
                          display: "flex",
                          alignItems: "center",
                          flexDirection: "column",
                          gap: 1,
                        }}
                      >
                        <SvgIcon fontSize="large" viewBox="0 0 50 50">
                          {item.icon}
                        </SvgIcon>
                        <Typography variant="h6">{item.title}</Typography>
                        <Typography
                          variant="body1"
                          sx={{ textAlign: "center" }}
                        >
                          {item.version}
                        </Typography>
                      </CardContent>
                    </Card>
                  </Grid>
                ))}
              </Grid>
            </Box>
          </Stack>
        </Box>

        <Box>
          <Stack direction={"column"} spacing={1}>
            <Typography variant="h3">Server</Typography>
            <Typography variant="body1">
              NMS Creative UI extends compatibility with Node.js, commencing
              from version 16.14 for server-side rendering. The aim is to
              provide support for Node.js versions, encompassing those in
              maintenance mode, until the latest release.
            </Typography>
          </Stack>
        </Box>

        <Box>
          <Stack direction={"column"} spacing={1}>
            <Typography variant="h3">React JS</Typography>
            <Typography variant="body1">
              NMS Creative UI is compatible with the latest iterations of React,
              beginning from version ^18.0.0. Refer to earlier versions for
              seamless backward compatibility.
            </Typography>
          </Stack>
        </Box>
      </Stack>
    </Box>
  );
};

export default SupportedPlatformsPage;
