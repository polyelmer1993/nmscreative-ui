import React from "react";
import { TEMPLATES } from "../../config/templatesList";
import {
  Box,
  Card,
  CardContent,
  CardMedia,
  Grid,
  Stack,
  Typography,
} from "../../components";
import { Link } from "react-router-dom";

const TemplatesPage = () => {
  return (
    <Box>
      <Stack direction={"column"} spacing={6}>
        <Box>
          <Stack direction={"column"} spacing={1}>
            <Typography variant="h2">Templates</Typography>
            <Typography variant="body1">
              Explore our assortment of complimentary React templates to
              initiate the development of your application using NMS Creative
              UI. This collection encompasses a React dashboard, React admin
              panel, and various other options.
            </Typography>
          </Stack>
        </Box>

        <Box sx={{ flexGrow: 1 }}>
          <Grid
            container
            spacing={{ mobile: 2, laptop: 6 }}
            columns={{ mobile: 1, tablet: 2, laptop: 2, desktop: 2 }}
          >
            {TEMPLATES.map((item) => (
              <Grid item mobile={1} tablet={1}>
                <Link to={item.path} style={{textDecoration:"none"}}>
                  <Card sx={{ "&.MuiPaper-elevation1": { padding: 0 } }}>
                    <CardMedia
                      component="img"
                      image={item.image}
                      alt="template image"
                    />
                    <CardContent sx={{ padding: "24px" }}>
                      <Typography gutterBottom variant="h5" component="div">
                        {item.title}
                      </Typography>
                      <Typography variant="body2" color="text.secondary">
                        {item.category}
                      </Typography>
                    </CardContent>
                  </Card>
                </Link>
              </Grid>
            ))}
          </Grid>
        </Box>
      </Stack>
    </Box>
  );
};

export default TemplatesPage;
