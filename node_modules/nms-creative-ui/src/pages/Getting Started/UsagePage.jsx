import React, { useState } from "react";
import {
  Box,
  Button,
  Paper,
  Stack,
  Tab,
  TabContext,
  TabList,
  TabPanel,
  Typography,
} from "../../components";
import CodeBlock from "../../lib/ReactSyntaxHighlighter";
import DashedPaper from "../../components/Generic Components/CustPaper/DashedPaper";

const jsx = `import React from "react";
import { Button } from "nms-creative-ui";

const ButtonUsage = () => {
  return <Button>Hello world</Button>;
};

export default ButtonUsage;`;

const UsagePage = () => {
  const [value, setValue] = useState(1);

  const handleChange = (event, newValue) => {
    setValue(newValue);
  };

  return (
    <Stack spacing={6}>
      <Box>
        <Stack direction={"column"} spacing={6}>
          <Stack direction={"column"} spacing={1}>
            <Typography variant="h2">Usage</Typography>
            <Typography variant="body1">
              Acquire fundamental proficiency in utilizing NMS Creative UI
              components.
            </Typography>
          </Stack>

          <Stack direction={"column"} spacing={1}>
            <Typography variant="h3">Quick start</Typography>
            <Typography variant="body1">
              Upon successful installation, you have the capability to import
              any NMS Creative UI component and commence exploration. For
              instance, experiment with altering the variant of the Button to
              "outlined" to observe the corresponding stylistic modifications:
            </Typography>
          </Stack>
        </Stack>
      </Box>

      <Stack direction="column" spacing={6}>
        <Paper sx={{ padding: "48px" }}>
          <TabContext value={value}>
            <TabList onChange={handleChange} aria-label="lab API tabs example">
              <Tab label="Preview" value={1} />
              <Tab label="JSX" value={2} />
            </TabList>

            <TabPanel value={1}>
            <DashedPaper>
                <Stack direction="row" spacing={4}>
                  <Box
                    sx={{
                      display: "flex",
                      justifyContent: "center",
                      width: "100%",
                    }}
                  >
                    <Button>Hello world</Button>
                  </Box>
                </Stack>
              </DashedPaper>
            </TabPanel>
            <TabPanel value={2}>
              <CodeBlock code={jsx} language={"jsx"} />
            </TabPanel>
          </TabContext>
        </Paper>
      </Stack>
    </Stack>
  );
};

export default UsagePage;
