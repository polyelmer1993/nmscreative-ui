import React from "react";
import { Paper } from "../../../../components";

const CustomPaper = ({ children, darkMode }) => { // Destructure darkMode from props
  return (
    <Paper 
      style={{
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
      justifyContent: "space-between",
        borderRadius: "20px",
        border: "2px dashed #919EAB40",
        backgroundColor: darkMode ? "transparent" : "#FBFBFC", // Use darkMode from props
        height: "100%",
        gap: "24px",
        padding: "24px",
        boxShadow: "none"
      }}
    >
      {children}
    </Paper>
  );
};

export default CustomPaper;