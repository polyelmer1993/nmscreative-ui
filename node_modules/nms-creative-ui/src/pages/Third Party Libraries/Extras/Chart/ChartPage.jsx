import React, { Fragment, useState } from "react";
import * as SolarIconSet from "solar-icon-set";
import COMPONENTS from "./data/components";


import { Box, Icon, Stack, Tab, TabContext, TabList, TabPanel, Typography } from "../../../../components";
import CodeBlock from "../../../../lib/ReactSyntaxHighlighter";
import CustPaper from "../../../../components/Generic Components/CustPaper/CustPaper";


const ChartTab = ({ item }) => {
  const [value, setValue] = useState(item.first_value);

  const handleChange = (event, newValue) => {
    setValue(newValue);
  };

  return (
    
      <CustPaper>
        <TabContext value={value}>
          <Typography variant="h4" sx={{ marginBottom: 3 }}>
            {item.name}
          </Typography>
          <TabList onChange={handleChange} aria-label="lab API tabs example">
            <Tab label="Preview" value={item.first_value} />
            <Tab label="JSX" value={item.second_value} />
          </TabList>

          <TabPanel value={item.first_value}>
            <Stack direction="row" spacing={4}>
              {React.createElement(item.component)}
            </Stack>
          </TabPanel>
          <TabPanel value={item.second_value}>
            <CodeBlock code={item.exampleComponent} language={"jsx"} />
          </TabPanel>
        </TabContext>
      </CustPaper>
   
  );
};

const ChartPage = () => {
  return (
    <Stack spacing={6}>
      <Box>
        <Stack direction={"column"} spacing={3}>
          <Stack direction={"row"} spacing={1}>
            <Icon color="secondary">
              <SolarIconSet.HandStars size={24} iconStyle="BoldDuotone" />
            </Icon>

            <Typography variant="body1">Extras</Typography>
          </Stack>

          <Stack direction={"column"} spacing={1}>
            <Typography variant="h2">Chart</Typography>
            <Typography variant="body1">
              Lorem ipsum dolor sit amet consectetur. Urna dictum pulvinar a
              orci eu nunc diam in neque.
            </Typography>
          </Stack>
          <Stack direction={"column"} spacing={1}>
            <a href="https://apexcharts.com/react-chart-demos/" target="_blank">
              <Typography>https://apexcharts.com/react-chart-demos/</Typography>
            </a>
          </Stack>
        </Stack>
      </Box>
      <Stack direction="column" spacing={6}>
        {COMPONENTS.map((item, index) => (
          <ChartTab key={index} item={item} />
        ))}
      </Stack>
    </Stack>
  );
};

export default ChartPage;
