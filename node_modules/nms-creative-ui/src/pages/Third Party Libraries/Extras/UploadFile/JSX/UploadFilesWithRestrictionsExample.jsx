export const UploadFilesWithRestrictionsExample = 
`import React from "react";
import { UploadFile } from "nms-creative-ui";

const UploadFilesWithRestrictions = () => {
  return (
    <UploadFile
      dragText={"Drag your files here"}
      defaultText={"Drop file here or click to upload"}
      acceptedFileTypes={["*.jpeg", "*.jpg", "*.png", "*.gif"]}
      maxFilesText={"Max 5 files and max of 10 MB"}
      multiple={true}
      fileRestriction={true}
      maxFiles={5}
    />
  );
};

export default UploadFilesWithRestrictions;`;
