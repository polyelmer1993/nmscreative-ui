import React from "react";
import { UploadFile } from "../../../../../components";

const UploadSingleFile = () => {
  return (
    <UploadFile
      dragText={"Drag your files here"}
      defaultText={"Drop file here or click to upload"}
      subtitle={"Drop file here or click browse thorough your machine"}
      fileRestriction={false}
      maxFiles={1}
    />
  );
};

export default UploadSingleFile;
